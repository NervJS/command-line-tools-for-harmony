import type { Node, TransformerFactory } from 'typescript';
import type { IOptions } from '../configs/IOptions';
export declare class TransformerManager {
    private static readonly sLoadPath;
    private readonly mTransformers;
    constructor(options: IOptions);
    getTransformers(): TransformerFactory<Node>[];
    private loadTransformers;
}
