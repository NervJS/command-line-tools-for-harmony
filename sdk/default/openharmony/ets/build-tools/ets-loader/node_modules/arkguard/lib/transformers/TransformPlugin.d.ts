import type { IOptions } from '../configs/IOptions';
import type { Node, TransformerFactory } from 'typescript';
export interface TransformPlugin {
    name: string;
    order: number;
    createTransformerFactory: (option: IOptions) => TransformerFactory<Node>;
}
export declare enum TransformerOrder {
    SHORTHAND_PROPERTY_TRANSFORMER = 0,
    RENAME_IDENTIFIER_TRANSFORMER = 1,
    VIRTUAL_CONSTRUCTOR_TRANSFORMER = 2,
    DISABLE_CONSOLE_TRANSFORMER = 3,
    RENAME_PROPERTIES_TRANSFORMER = 4,
    RENAME_FILE_NAME_TRANSFORMER = 5
}
