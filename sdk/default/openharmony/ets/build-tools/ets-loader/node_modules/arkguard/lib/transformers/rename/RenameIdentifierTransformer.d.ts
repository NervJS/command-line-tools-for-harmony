import type { Identifier, Node } from 'typescript';
import type { TransformPlugin } from '../TransformPlugin';
declare namespace secharmony {
    let transformerPlugin: TransformPlugin;
    let nameCache: Map<string, string | Map<string, string>>;
    let historyNameCache: Map<string, string>;
    let historyUnobfuscatedNamesMap: Map<string, string[]>;
    let identifierLineMap: Map<Identifier, string>;
    let classMangledName: Map<Node, string>;
    let classInfoInMemberMethodCache: Set<string>;
    function clearCaches(): void;
}
export = secharmony;
